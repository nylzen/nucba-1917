import React from 'react';
// import { useContext } from 'react';
// import { Contexto } from '../../context';

const About = () => {
  // const { color } = useContext(Contexto);
  // return <h2 style={{ color: color }}>About</h2>;
  return <h2>Holis about</h2>;
};

export default About;
