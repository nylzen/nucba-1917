import React from 'react';
import { SmallWrapper } from './SmallContainerStyles';

const SmallContainer = ({ children }) => {
  return <SmallWrapper>{children}</SmallWrapper>;
};

export default SmallContainer;
